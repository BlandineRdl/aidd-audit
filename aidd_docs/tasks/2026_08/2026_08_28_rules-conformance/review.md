# Review: existing code against `.claude/rules/`

- **Verdict**: changes-requested
- **Diff**: `f8fd3cd...e08e301` (whole tracked tree under `src/`, `tests/`, `scripts/`)
- **Axes run**: code, relevancy
- **Rules read**: `.claude/rules/00-architecture/0-use-cases.md` (scoped `src/**/*.usecase.ts`, so it governs `check-maturity.usecase.ts` alone), `.claude/rules/01-standards/1-clean-code.md` (scoped `src/**/*.ts`, `tests/**/*.ts`, `scripts/**/*.mjs`)
- **Date**: 2026_08_28
- **Findings**: 0 critical, 6 warning, 5 minor

## Phases

Not run. No plan holds acceptance criteria for this work; the request was a rules-conformance pass over existing code, not a trace against phases.

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🟡 | conform | - | `src/maturity/usecases/check-maturity.usecase.ts:52,61,128,139,146,159,169` | The use case is the validator of the maturity model. Six of the seven `MaturityModelError` throws concern the *model*, not the observations: unknown axis in a requirement, level silent on an axis, set/non-set requirement mismatch, non-numeric threshold, threshold off scale, unknown axis or scale. All can only originate from `aidd.yml` or `--model`. Breaks `0-use-cases.md` § A use case must not ("Validate a whole configuration or model.") and § Domain inputs ("Never defend against malformed domain models." / "Validate in the adapter, parser, or loader."). | Move model-shape validation into `yaml-maturity-model.adapter` behind `MaturityModelPort`, throwing the already-declared `MaturityModelLoadError`. Keep in the use case only the two application-input rejections: duplicate observation (`:40`) and observed value off its scale (`:154`). Move `check-maturity.usecase.test.ts:228-250` to the adapter suite; keep `:223` and `:260`. **Conflicts with `architecture.md` § Frozen before the split and with commit `a3a36c8`, which chose this placement deliberately — arbitrate before acting.** |
| 🟡 | conform | - | `src/maturity/usecases/check-maturity.usecase.ts:34-90` | The exported function does not read like the business flow. It builds an observation index with a duplicate check, builds a Set of declared axis ids, sorts levels, then nests two `.map()` around an inner validation loop (4 levels of indentation) before deriving `proven` and `next`. Breaks `0-use-cases.md` § Structure ("The exported function reads as business flow." / "Keep it small enough to scan." / "Never inline parsing, comparison, or validation mechanics.") and `1-clean-code.md` § Functions ("never mix orchestration, mapping and domain decisions"). | Extract `indexObservations`, `evaluateLevel(model, level, observed)`, `highestProven(levels)` and `levelAbove(levels, proven)`. The body then reads: index, evaluate each level, take the highest proven, take the one above. |
| 🟡 | conform | - | `src/maturity/usecases/check-maturity.usecase.ts:122-165` | `reaches()` carries three scale kinds, five shape guards and the comparison itself in 44 lines. Breaks `1-clean-code.md` § Functions ("one decision per function", "keep branching local to its rule"). | Once the guards move out per the first finding, dispatch on `scale.kind` into `setSatisfied`, `numericSatisfied`, `ordinalSatisfied`. |
| 🟡 | rot | - | `src/maturity/models/axis-observation.model.ts:10`, `src/evidence/models/observation.model.ts:36`, `src/assessment/contracts/assessment-report.contract.ts:17` | The four evidence-status names exist in three independent copies, and nothing pins them equal. The separation is justified (peers never import each other; the contract is self-contained), but the justification covers why they are separate, never why they may drift. A member added to one and not the others compiles, and the divergence surfaces at composition time — the exact failure `architecture.md` § Frozen before the split exists to prevent. Same shape for `ObservedValue` (`axis-observation.model.ts:14`, `observation.model.ts:18`) and `AxisId = string` (`maturity.model.ts:1`, `axis.model.ts:9`). | Add a conformance test under `tests/assessment/` asserting the three sets are equal, in the one place allowed to import all three. Keep the boundary; pin the equality. |
| 🟡 | fit | - | `src/maturity/usecases/check-maturity.usecase.ts:83` | `proven` is the highest level whose outcome is `MET`, whatever the levels below it did. `project-brief.md` defines a level as one of seven *cumulative* ranks and `testing.md` § Level says "report the highest fully satisfied lower level". `aidd.yml` happens to be monotone, so nothing surfaces — but `--model` is a supported flag (`cli.md`) and neither the engine nor `aidd-model.test.ts` enforces monotonicity, so a custom model with a non-monotone rung reports a level whose predecessors are `NOT_MET`, silently. | Either pin monotonicity as a model invariant at the loading boundary (same place as the first finding) and state it, or make `proven` the highest level whose predecessors are all `MET`. Add the test either way. |
| 🟡 | code | - | `scripts/prove-boundary-rules.mjs:158-166`, `package.json:architecture` | An interrupted run leaves sentinels in `src/`: `finally` does not run on SIGINT/SIGTERM. The start-of-run sweep at `:158` cannot recover, because `architecture` is `depcruise src && node scripts/prove-boundary-rules.mjs` — the leftover sentinel makes `depcruise` exit 1, `&&` short-circuits, and the sweep never executes. Verified: dropping `src/maturity/usecases/__boundary-sentinel__fs.ts` yields `error domain-has-no-filesystem` and `[ELIFECYCLE] Command failed with exit code 1`, and the file survives the run. `pnpm check` stays red until a human deletes it by hand. | Register `process.on('SIGINT'\|'SIGTERM', cleanup)`, or run the prover first (`node scripts/prove-boundary-rules.mjs && depcruise src`) so the sweep always gets its turn. |
| 🟢 | conform | - | `src/maturity/usecases/check-maturity.usecase.ts:97,46` | Technical names where the domain has words (`1-clean-code.md`, "prefer domain names over technical names"). `resolve()` returns a requirement `Outcome`, but `resolve` is already the evidence context's verb for a different operation — "several observations to one status" (`observation.model.ts:8`) versus "one requirement to MET/NOT_MET" here. `known` names a Set, not a concept. | `outcomeOf(...)` and `declaredAxes`. |
| 🟢 | rot | - | `src/maturity/ports/maturity-model.port.ts:15` | `MaturityModelLoadError` is declared and thrown nowhere; the use case throws `MaturityModelError` instead. Two error types for one category, and `no-orphans` cannot catch it because `ports/` is exempted (`.dependency-cruiser.cjs:88`). | Falls out of the first finding: the adapter throws it. If that finding is refused, delete the class. |
| 🟢 | code | - | `src/maturity/usecases/check-maturity.usecase.ts:136` | `(value as readonly string[])` immediately after `Array.isArray(value)` — a cast standing in for a narrowing that `Array.isArray` does not perform on a readonly union member. `1-clean-code.md` § State: reject impossible states at boundaries. | Name the guard: `const isSet = (v: ObservedValue): v is readonly string[] => Array.isArray(v)`. The cast disappears. |
| 🟢 | code | - | `tests/maturity/aidd-model.test.ts:19,104`, `tests/maturity/check-maturity.usecase.test.ts:59,70` | Fixture builders erase their own types: `as never` on `confirmed()` and on the UNKNOWN observation, `unknown` in the `observe()` override map with `as AxisObservation['value']` on the way out. A malformed fixture compiles, so the tests stop defending the shape they exercise. `1-clean-code.md` covers `tests/**/*.ts`. | Type the helpers against the domain: `confirmed(axis: AxisId, value: ObservedValue): AxisObservation`, and make the override map `Partial<Record<AxisId, [ObservedValue \| null, EvidenceConfidence]>>`. |
| 🟢 | code | - | `tests/maturity/check-maturity.usecase.test.ts:74` | `requirementOf` returns an `AxisResult`, not a `RequirementResult`, and every call site reads `.outcome` off an axis. The name misdirects the reader about what the assertions cover. | Rename to `axisOf`. |

## Verification

Not run. No plan and no acceptance criteria were in scope.
